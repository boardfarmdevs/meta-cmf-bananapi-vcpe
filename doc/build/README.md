# Other builds

This directory contains non-EasyMesh build material only.

To build the EasyMesh BPI images, create a named LXD lab VM, or select a test
tier, start at [doc/easymesh/build](../easymesh/build/README.md). That guide owns
the single pinned upstream manifest and all EasyMesh build scripts.
